<div class="ui grid stackable centered">

  <div class="ten wide column" style="margin-top: 2vh;">
    <div class="ui very padded segment stacked" style="background: white; opacity: 0.9">

      <div class="ui row">
        <h2 class="ui icon header center aligned icon header">
          <i class="envelope icon"></i>
          <div class="content">
            Requests
          </div>
        </h2>
      </div>

      <% current_user.requests.each do |req| %>
      <div class="ui row" style="margin-bottom: 3vh;">
        <div class="ui segment center aligned">
          <h3>Looking for <%= req.item_name %></h3>
          <h5><%= req.description %></h5>
          <%= link_to 'Delete', request_path(req), method: :delete, class: 'ui fluid button red basic' %>
        </div>
      </div>
      <% end %>

    </div>
  </div>

</div>
